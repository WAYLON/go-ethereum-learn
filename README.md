# 学习区块链技术
## 智能合约
## ERC20代币

```
git flow feature start optimize_search_email_etc --showcommands 

git flow feature publish optimize_search_email_etc --showcommands 

git flow feature finish optimize_search_email_etc --showcommands 


git flow release start v1.5.0 --showcommands 

git flow release publish v1.5.0 --showcommands 

git flow release finish v1.5.0 --showcommands 


git flow hotfix start v1.5.1 --showcommands 

git flow hotfix publish v1.5.1 --showcommands 

git flow hotfix finish v1.5.1 --showcommands 

git commit -a -m "www"

git branch -a 
git push origin :
git push origin :release

git tag
git push --tags

git push origin --delete feature/optimize_search_email_etc

```